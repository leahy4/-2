
	<?php
	// Create an array and push 5 elements on to it, then 
	$fam_mem = array();
	array_push($fam_mem, "Pat");
    array_push($fam_mem, "Payton");
    array_push($fam_mem, "Sean");
    array_push($fam_mem, "Kari");
    array_push($fam_mem, "Buddi");
    
    Print count($fam_mem);
    
    
    // print the number of elements in your array to
    
    //the screen

	?>